[pytest]
# このレポジトリ自身のテストでは収集プラグインを外す。
# 付けたままだと、テストを走らせるたびに tests/ の実行が試行として
# キューに積まれる。
#
# pyproject.toml ではなくここに置くのは、`lpptest --run-pytest` で
# 開発中にホスト実行したとき rootdir の探索でこの設定を拾われ、
# 収集が黙って止まるのを避けるためである。
addopts = -p no:lpp_collector
testpaths = .
